import styled from 'styled-components';

const SimilarLandmark = () => {
  return <LandmarkContainer></LandmarkContainer>;
};

export default SimilarLandmark;

const LandmarkContainer = styled.div`
  width: 100%;
  background-color: gray;
`;
